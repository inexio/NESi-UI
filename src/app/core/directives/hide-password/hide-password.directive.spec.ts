import { HidePasswordDirective } from './hide-password.directive';

describe('HidePasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new HidePasswordDirective();
    expect(directive).toBeTruthy();
  });
});
