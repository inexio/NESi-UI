import { PlaceholderWidthDirective } from './placeholder-width.directive';

describe('PlaceholderWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceholderWidthDirective();
    expect(directive).toBeTruthy();
  });
});
