import { CodeCopyDirective } from "./code-copy.directive";

describe("CodeCopyDirective", () => {
    it("should create an instance", () => {
        const directive = new CodeCopyDirective();
        expect(directive).toBeTruthy();
    });
});
